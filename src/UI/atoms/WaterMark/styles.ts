import { SxProps } from '@mui/material';

export const styles: Record<string, SxProps> = {
  root: {
    position: 'absolute',
    right: '-20px',
    top: '50%',
    transform: 'rotate(270deg)',
    opacity: 0.1,
    userSelect: 'none',
  },
};
