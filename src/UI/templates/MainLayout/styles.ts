import { SxProps } from '@mui/material';

export const styles: Record<string, SxProps> = {
  root: {
    flexGrow: 1,
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    pt: { xs: '110px', md: '64px' },
  },
  main: {
    flexGrow: 1,
  },
};
